apiVersion: v1
kind: Service
metadata:
  name: bakoda-service
  labels:
    app: bakoda
spec:
  type: NodePort
  selector:
    app: bakoda
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
